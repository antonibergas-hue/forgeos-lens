# PR12148-416 — Add a hello CLI utility

## Problem
Create a minimal CLI utility for the forgeos-lens repository that outputs a friendly greeting message along with the current UTC time.

## Proposed solution
Add a new Python script (e.g., `hello.py` or `cli_hello.py`) in the repository that uses the `datetime` module to fetch UTC time and prints a simple, welcoming message. Include basic CLI argument parsing (`argparse`) for potential future extensions, along with a README section documenting usage.

## Definition of Done
- [ ] Script runs successfully with `python hello.py` (or equivalent) and prints a greeting with UTC time
- [ ] Code is well-documented with docstrings and inline comments
- [ ] PR includes a proposal+DoD doc as requested

## JIRA
PR12148-416